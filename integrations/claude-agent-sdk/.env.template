LLM_API_KEY=                      # OpenAI API key, see other providers here: https://docs.cognee.ai/setup-configuration/llm-providers 
LLM_MODEL=                        # default: gpt-5
ENABLE_BACKEND_ACCESS_CONTROL=    # see details: https://docs.cognee.ai/core-concepts/permissions-system/overview#cognee-permissions-system

# The Claude Agent SDK uses a bundled Claude Code CLI that handles authentication automatically. You do NOT need to set ANTHROPIC_API_KEY if:
# - In Cursor: Uses your existing Cursor/Claude session credentials
# - Standalone: First run guides you through OAuth or API key authentication
# For CI/CD or automated environments, you may need to authenticate the CLI
# separately. See: https://github.com/anthropics/claude-agent-sdk-python
